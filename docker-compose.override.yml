services:
  backend:
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:5000
      Security__EnforceHttps: false
      Jwt__SigningKey: DEV_ONLY_CHANGE_ME_TO_A_LONG_RANDOM_SECRET
      Security__BootstrapAdmin__Password: P@ssw0rd!
      Cors__AllowedOrigins__0: http://localhost:5173
      Database__ConnectionString: Data Source=/app/data/atlas-dev.db
    ports:
      - "5000:5000"

  frontend:
    ports:
      - "5173:80"

  nginx:
    profiles:
      - gateway
