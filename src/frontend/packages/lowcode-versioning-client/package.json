{
  "name": "@atlas/lowcode-versioning-client",
  "private": true,
  "version": "0.0.0",
  "description": "Atlas 低代码版本管理（M14）—— 时间线 + diff + rollback + 资源引用反查 + FAQ 入口。",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "lint": "echo '@atlas/lowcode-versioning-client: lint via root pipeline'",
    "build": "echo '@atlas/lowcode-versioning-client: source-only package, no build step'",
    "test": "vitest --run --passWithNoTests"
  },
  "devDependencies": { "vitest": "^4.1.4" }
}
