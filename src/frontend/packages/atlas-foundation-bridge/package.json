{
  "name": "@atlas/foundation-bridge",
  "version": "0.0.1",
  "description": "Atlas 宿主到 Coze foundation-sdk 的桥接层：把 Coze 期望的 useUserInfo/useLoginStatus/useCurrentTheme/useSpace 等 API 实现转接到 Atlas 已有的认证、工作区、主题状态上，避免引入 cozelib 自带的账号/空间 store。",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./atlas-host-bridge": "./src/atlas-host-bridge.ts"
  },
  "scripts": {
    "build": "exit 0",
    "lint": "exit 0",
    "test": "exit 0"
  },
  "dependencies": {
    "@atlas/shared-react-core": "workspace:*"
  }
}
