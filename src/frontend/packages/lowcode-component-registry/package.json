{
  "name": "@atlas/lowcode-component-registry",
  "private": true,
  "version": "0.0.0",
  "description": "Atlas 低代码组件注册表（M06 C06-1 / C06-2）—— registerComponent / getRegistry / 元数据驱动校验器（CI 守门，禁止组件实现内硬编码 fetch / workflow 直调）。",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./principles": "./src/principles/index.ts"
  },
  "scripts": {
    "lint": "echo '@atlas/lowcode-component-registry: lint via root pipeline'",
    "build": "echo '@atlas/lowcode-component-registry: source-only package, no build step'",
    "test": "vitest --run --passWithNoTests"
  },
  "dependencies": {
    "@atlas/lowcode-schema": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
