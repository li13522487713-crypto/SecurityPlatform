## 前端环境变量示例（复制为 .env.local 后修改）
#
# 默认 API 前缀（Vite dev proxy 通常用 /api）
VITE_API_BASE=/api
#
# 默认租户ID（GUID）。用于本地启动时自动写入 localStorage.tenant_id
# 例如：VITE_DEFAULT_TENANT_ID=00000000-0000-0000-0000-000000000001
VITE_DEFAULT_TENANT_ID=00000000-0000-0000-0000-000000000001
